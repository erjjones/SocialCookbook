<div class="page-header">
	<h1>Edit recipe</h1>
</div>

<%= render 'form' %>

<%= link_to('Add more ingredients', @recipe, :class => 'btn') %> <%= link_to('Go back to dashboard', recipes_path, :class => 'btn') %> 